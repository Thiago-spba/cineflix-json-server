services:
  - type: web
    name: cineflix-json-server
    env: node
    buildCommand: ""
    startCommand: json-server --watch db.json --port 8080
    envVars:
      - key: NODE_ENV
        value: production
